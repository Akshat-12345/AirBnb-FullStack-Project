<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

<style>
  :root {
    /* Exact Logo Palette */
    --logo-teal: #1EBBD7;
    --logo-blue: #1D78D6;
    --logo-yellow: #F5B800;
    --logo-orange: #F26C23;
    --logo-pink: #E32D76;
    --logo-purple: #7A36A8;
    
    --brand-spectrum: linear-gradient(135deg, #1EBBD7 0%, #1D78D6 20%, #F5B800 40%, #F26C23 60%, #E32D76 80%, #7A36A8 100%);
    --brand-spectrum-h: linear-gradient(90deg, #1EBBD7 0%, #1D78D6 20%, #F5B800 40%, #F26C23 60%, #E32D76 80%, #7A36A8 100%);
    --brand-spectrum-v: linear-gradient(180deg, #1EBBD7 0%, #1D78D6 20%, #F5B800 40%, #F26C23 60%, #E32D76 80%, #7A36A8 100%);

    --nav-bg: rgba(255, 255, 255, 0.92);
    --nav-border: rgba(30, 187, 215, 0.12);
    --nav-text: #1f242e;
    --nav-muted: #64748b;
    --nav-shadow: 0 10px 30px -10px rgba(29, 120, 214, 0.08);
    --nav-shadow-scrolled: 0 12px 35px -5px rgba(242, 108, 35, 0.12);
  }

  /* === 🌐 1. EXACT LOGO PALETTE SCROLLBAR === */
  html, body, * {
    scrollbar-width: thin;
    scrollbar-color: var(--logo-teal) #f8fafc;
  }

  ::-webkit-scrollbar {
    width: 9px;
    height: 9px;
  }

  ::-webkit-scrollbar-track {
    background: #f8fafc;
    border-radius: 100px;
    margin: 3px 0;
  }

  /* Vertical Thumb: Smooth drip spectrum matching the paint strips */
  ::-webkit-scrollbar-thumb {
    background: var(--brand-spectrum-v);
    border-radius: 100px;
    border: 2px solid #ffffff;
    box-shadow: 0 1px 5px rgba(0, 0, 0, 0.15);
  }

  /* Horizontal Thumb */
  ::-webkit-scrollbar-thumb:horizontal {
    background: var(--brand-spectrum-h);
    border-radius: 100px;
    border: 2px solid #ffffff;
  }

  ::-webkit-scrollbar-thumb:hover {
    filter: brightness(1.1);
    box-shadow: 0 0 10px rgba(30, 187, 215, 0.4);
  }

  ::-webkit-scrollbar-thumb:horizontal:hover {
    filter: brightness(1.1);
  }

  /* Modals & Dropdown scrollbars */
  .currency-grid-deck::-webkit-scrollbar,
  .atithi-autocomplete-panel::-webkit-scrollbar,
  .atithi-inline-filter-dropdown::-webkit-scrollbar {
    width: 6px;
  }
  
  .currency-grid-deck::-webkit-scrollbar-thumb,
  .atithi-autocomplete-panel::-webkit-scrollbar-thumb,
  .atithi-inline-filter-dropdown::-webkit-scrollbar-thumb {
    background: var(--brand-spectrum-v);
    border-radius: 100px;
    border: 1px solid #ffffff;
  }

  /* === 🌌 2. FLUID SHRINK-ON-SCROLL NAVBAR === */
  .atithi-navbar-master {
    position: sticky !important;
    top: 0;
    left: 0;
    right: 0;
    z-index: 99999 !important;
    height: 100px;
    background: var(--nav-bg) !important;
    backdrop-filter: blur(24px) saturate(180%);
    -webkit-backdrop-filter: blur(24px) saturate(180%);
    border-bottom: 1px solid var(--nav-border);
    box-shadow: var(--nav-shadow);
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    padding: 0 4rem;
    gap: 16px;
    transition: height 0.3s cubic-bezier(0.16, 1, 0.3, 1), 
                box-shadow 0.3s ease, 
                padding 0.3s ease,
                background 0.3s ease;
    box-sizing: border-box;
  }

  .atithi-navbar-master.navbar-scrolled {
    height: 82px;
    box-shadow: var(--nav-shadow-scrolled);
    background: rgba(255, 255, 255, 0.98) !important;
    padding: 0 3.5rem;
  }

  /* Top laser spectrum line with animated glow */
  .atithi-navbar-master::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: var(--brand-spectrum-h);
    z-index: 100000;
  }

/* Brand Logo - Clean & Seamless */
  .brand-identity-anchor {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none !important;
    cursor: pointer;
    flex-shrink: 0;
    background: transparent !important;
  }
  
  .brand-logo-img {
    height: 50px;
    width: auto;
    object-fit: contain;
    /* White background ko navbar ke sath seamlessly blend karta hai */
    mix-blend-mode: multiply; 
    filter: none !important;
    border: none !important;
    box-shadow: none !important;
    background: transparent !important;
    transition: transform 0.25s ease;
  }

  .navbar-scrolled .brand-logo-img {
    height: 36px;
  }

  /* Simple subtle scale on hover without weird tilts/shadows */
  .brand-identity-anchor:hover .brand-logo-img {
    transform: scale(1.05);
  }

  .brand-logo-text {
    font-weight: 700;
    font-size: 30px;
    letter-spacing: -0.5px;
    /* Exactly matching the teal & orange blend of the logo */
    background: linear-gradient(135deg, #1EBBD7 10%, #F5B800 50%, #F26C23 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: inline-block;
    transition: font-size 0.3s ease;
  }

  .navbar-scrolled .brand-logo-text {
    font-size: 24px;
  }
  .brand-identity-anchor:hover .brand-logo-img {
    transform: rotate(-8deg) scale(1.12);
    filter: drop-shadow(0 6px 12px rgba(242, 108, 35, 0.35));
  }
  .brand-logo-text {
    font-weight: 700;
    font-size: 35px;
    letter-spacing: -0.3px;
    background: var(--brand-spectrum);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: inline-block;
    transition: font-size 0.3s ease;
  }
  .navbar-scrolled .brand-logo-text {
    font-size: 26px;
  }

  /* === 🔍 3. SEARCH CAPSULE === */
  .center-capsule-scaffold {
    display: flex;
    justify-content: center;
    position: relative;
    width: 100%;
  }

  .atithi-search-pill-hub {
    position: relative;
    display: flex;
    align-items: center;
    background: #ffffff;
    border: 1.5px solid #eef2f6;
    border-radius: 40px;
    padding: 6px 8px 6px 20px;
    box-shadow: 0 4px 18px rgba(15, 23, 42, 0.05);
    transition: all 0.3s cubic-bezier(0.2, 0, 0, 1);
    max-width: 100%;
  }
  .navbar-scrolled .atithi-search-pill-hub {
    padding: 5px 6px 5px 18px;
    box-shadow: 0 3px 12px rgba(15, 23, 42, 0.07);
  }
  .atithi-search-pill-hub:hover {
    border-color: rgba(30, 187, 215, 0.4);
    box-shadow: 0 8px 24px rgba(30, 187, 215, 0.12);
    transform: translateY(-1px);
  }
  .atithi-search-pill-hub:focus-within {
    border-color: var(--logo-blue);
    box-shadow: 0 10px 30px rgba(29, 120, 214, 0.18);
  }

  .search-inline-form {
    display: flex;
    align-items: center;
    gap: 6px;
    margin: 0;
    width: 100%;
  }

  .main-search-input {
    border: none;
    outline: none;
    background: transparent;
    font-size: 14.5px;
    font-weight: 600;
    color: #1e293b;
    width: 270px;
    transition: width 0.3s ease;
  }
  .main-search-input:focus {
    width: 310px;
  }
  .main-search-input::placeholder {
    color: #94a3b8;
    font-weight: 500;
  }

  .capsule-v-divider {
    color: #cbd5e1;
    font-weight: 300;
    font-size: 16px;
    user-select: none;
  }

  .btn-voice-mic-trigger {
    background: transparent;
    border: none;
    color: #64748b;
    font-size: 15px;
    padding: 6px 8px;
    cursor: pointer;
    border-radius: 50%;
    transition: all 0.25s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .btn-voice-mic-trigger:hover {
    color: var(--logo-pink);
    background: rgba(227, 45, 118, 0.08);
  }
  .btn-voice-mic-trigger.recording-active {
    color: var(--logo-pink);
    background: rgba(227, 45, 118, 0.15);
    animation: micPulse 1s infinite alternate;
  }
  @keyframes micPulse {
    0% { transform: scale(1); filter: drop-shadow(0 0 2px var(--logo-pink)); }
    100% { transform: scale(1.22); filter: drop-shadow(0 0 8px var(--logo-pink)); }
  }

  .btn-capsule-filter-toggle {
    background: transparent;
    border: none;
    outline: none;
    font-size: 13.5px;
    font-weight: 600;
    color: #334155;
    padding: 6px 14px;
    border-radius: 20px;
    cursor: pointer;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    gap: 6px;
    white-space: nowrap;
  }
  .btn-capsule-filter-toggle:hover, .btn-capsule-filter-toggle.active {
    background: rgba(30, 187, 215, 0.08);
    color: var(--logo-teal);
  }

  /* Vibrant Paint-Gradient Search Button */
  .btn-search-magnifier {
    background: linear-gradient(135deg, var(--logo-orange) 0%, var(--logo-pink) 100%);
    color: white;
    width: 38px;
    height: 38px;
    border-radius: 50%;
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    cursor: pointer;
    box-shadow: 0 4px 14px rgba(242, 108, 35, 0.35);
    transition: all 0.25s cubic-bezier(0.34, 1.56, 0.64, 1);
    flex-shrink: 0;
  }
  .navbar-scrolled .btn-search-magnifier {
    width: 34px;
    height: 34px;
    font-size: 12px;
  }
  .btn-search-magnifier:hover {
    transform: scale(1.1) rotate(8deg);
    box-shadow: 0 6px 20px rgba(227, 45, 118, 0.45);
  }

  /* === AUTOCOMPLETE & FILTERS === */
  .atithi-autocomplete-panel {
    position: absolute;
    top: calc(100% + 14px);
    left: 0;
    right: 0;
    background: #ffffff;
    border: 1px solid #eef2f6;
    border-radius: 24px;
    box-shadow: 0 20px 50px rgba(15, 23, 42, 0.14);
    max-height: 420px;
    overflow-y: auto;
    z-index: 100002;
    padding: 12px;
    display: none;
  }

  .suggestion-item {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 10px 14px;
    border-radius: 16px;
    cursor: pointer;
    transition: all 0.2s ease;
    border-bottom: 1px solid #f8fafc;
  }
  .suggestion-item:hover {
    background: rgba(30, 187, 215, 0.05);
    transform: translateX(4px);
  }
  .suggestion-thumb {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    object-fit: cover;
    flex-shrink: 0;
    border: 1.5px solid #e2e8f0;
  }
  .suggestion-title {
    font-size: 14.5px;
    font-weight: 600;
    color: #1e293b;
  }
  .suggestion-subtitle {
    font-size: 12.5px;
    color: #64748b;
  }
  .suggestion-price {
    font-size: 13px;
    font-weight: 700;
    color: var(--logo-blue);
  }

  .atithi-inline-filter-dropdown {
    position: absolute;
    top: calc(100% + 14px);
    right: 0;
    width: 540px;
    max-width: 90vw;
    background: #ffffff;
    border: 1px solid #eef2f6;
    border-radius: 26px;
    box-shadow: 0 25px 60px rgba(15, 23, 42, 0.16);
    padding: 24px;
    display: none;
    z-index: 100001;
  }

  .filter-section-header {
    font-size: 15px;
    font-weight: 700;
    color: #1e293b;
    margin-bottom: 12px;
  }
  .price-slider-label {
    font-size: 13px;
    font-weight: 700;
    color: var(--logo-orange);
    background: rgba(242, 108, 35, 0.08);
    padding: 4px 14px;
    border-radius: 20px;
    border: 1px solid rgba(242, 108, 35, 0.2);
  }
  .atithi-filter-slider {
    width: 100%;
    accent-color: var(--logo-orange);
    cursor: pointer;
  }
  .category-pill-matrix {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .pill-check-input { display: none; }
  .pill-check-label {
    font-size: 13px;
    font-weight: 500;
    color: #475569;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    padding: 7px 16px;
    border-radius: 20px;
    cursor: pointer;
    transition: all 0.25s ease;
  }
  .pill-check-label:hover {
    border-color: var(--logo-teal);
    color: var(--logo-teal);
    background: #ffffff;
  }
  .pill-check-input:checked + .pill-check-label {
    background: linear-gradient(135deg, var(--logo-blue) 0%, var(--logo-teal) 100%);
    color: #ffffff;
    border-color: transparent;
    font-weight: 600;
    box-shadow: 0 4px 12px rgba(29, 120, 214, 0.25);
  }
  .filter-actions-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 18px;
    border-top: 1px solid #f1f5f9;
    margin-top: 20px;
  }
  .filter-clear-link {
    font-size: 14px;
    font-weight: 600;
    color: #64748b;
    text-decoration: underline;
    cursor: pointer;
    transition: color 0.2s;
  }
  .filter-clear-link:hover {
    color: var(--logo-pink);
  }
  .btn-filter-apply {
    background: linear-gradient(135deg, var(--logo-orange) 0%, var(--logo-pink) 100%);
    color: #ffffff;
    border: none;
    font-size: 14px;
    font-weight: 600;
    padding: 10px 24px;
    border-radius: 12px;
    cursor: pointer;
    box-shadow: 0 4px 14px rgba(227, 45, 118, 0.3);
    transition: all 0.2s;
  }
  .btn-filter-apply:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 18px rgba(227, 45, 118, 0.4);
  }

  /* === 👤 4. RIGHT UTILITY CONTROLS === */
  .right-utility-deck {
    display: flex;
    align-items: center;
    gap: 12px;
    justify-content: flex-end;
  }
  .nav-host-link {
    font-size: 14px;
    font-weight: 600;
    color: #334155;
    text-decoration: none !important;
    padding: 10px 16px;
    border-radius: 24px;
    white-space: nowrap;
    transition: all 0.2s;
  }
  .nav-host-link:hover {
    background: rgba(30, 187, 215, 0.08);
    color: var(--logo-teal);
  }
  .currency-globe-btn {
    background: transparent;
    border: none;
    color: #334155;
    font-weight: 600;
    font-size: 14px;
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 8px 14px;
    border-radius: 24px;
    cursor: pointer;
    white-space: nowrap;
    transition: all 0.2s;
  }
  .currency-globe-btn:hover {
    background: rgba(29, 120, 214, 0.08);
    color: var(--logo-blue);
  }
  .currency-globe-btn i {
    color: var(--logo-blue);
  }

  .profile-trigger-pill {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 5px 6px 5px 14px;
    background: #ffffff;
    border: 1.5px solid #e2e8f0;
    border-radius: 30px;
    cursor: pointer;
    box-shadow: 0 2px 6px rgba(15, 23, 42, 0.04);
    transition: all 0.25s ease;
  }
  .profile-trigger-pill:hover {
    border-color: var(--logo-purple);
    box-shadow: 0 4px 14px rgba(122, 54, 168, 0.15);
  }
  .profile-avatar-node {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--logo-purple) 0%, var(--logo-pink) 100%);
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    font-weight: 700;
    overflow: hidden;
    box-shadow: 0 2px 8px rgba(122, 54, 168, 0.25);
  }
  .profile-avatar-node img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .dropdown-menu-atithi {
    border-radius: 20px;
    box-shadow: 0 16px 45px rgba(15, 23, 42, 0.15);
    padding: 10px;
    min-width: 250px;
    margin-top: 12px !important;
    border: 1px solid #eef2f6;
    background: #ffffff;
  }
  .dropdown-menu-atithi .dropdown-item {
    padding: 10px 14px;
    font-size: 14px;
    font-weight: 500;
    color: #334155;
    border-radius: 12px;
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    transition: all 0.15s;
  }
  .dropdown-menu-atithi .dropdown-item:hover {
    background: #f8fafc;
    color: var(--logo-blue);
    transform: translateX(3px);
  }

  /* Currency Modal */
  .atithi-currency-modal-backdrop {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(15, 23, 42, 0.5);
    backdrop-filter: blur(8px);
    z-index: 100005;
    display: none;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }
  .atithi-currency-modal-backdrop.active-curr-modal {
    display: flex;
  }
  .currency-modal-sheet {
    background: #ffffff;
    width: 100%;
    max-width: 740px;
    border-radius: 26px;
    box-shadow: 0 25px 60px rgba(15, 23, 42, 0.22);
    padding: 28px;
  }
  .currency-modal-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    padding-bottom: 12px;
    border-bottom: 1px solid #f1f5f9;
  }
  .currency-grid-deck {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    max-height: 400px;
    overflow-y: auto;
  }
  .currency-option-card {
    padding: 12px 16px;
    border-radius: 14px;
    border: 1.5px solid #eef2f6;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .currency-option-card:hover {
    border-color: var(--logo-teal);
    background: rgba(30, 187, 215, 0.04);
  }
  .currency-option-card.selected-curr {
    border-color: var(--logo-orange);
    background: rgba(242, 108, 35, 0.08);
    box-shadow: 0 2px 10px rgba(242, 108, 35, 0.15);
  }

  /* ========================================================
     📱 RESPONSIVE RULES: TABLET & MOBILE VIEWPORTS
     ======================================================== */
  @media (max-width: 1024px) and (min-width: 768px) {
    .atithi-navbar-master {
      padding: 0 1.5rem;
      height: 90px;
    }
    .atithi-navbar-master.navbar-scrolled {
      padding: 0 1.25rem;
      height: 78px;
    }
    .center-capsule-scaffold {
      display: flex !important;
    }
    .main-search-input {
      width: 150px;
    }
    .main-search-input:focus {
      width: 180px;
    }
    .currency-globe-btn {
      display: flex !important;
      padding: 6px 10px;
      font-size: 13px;
    }
    .nav-host-link {
      display: none !important;
    }
  }

  @media (max-width: 767px) {
    .atithi-navbar-master {
      padding: 0 12px;
      height: 76px;
      gap: 8px;
    }
    .atithi-navbar-master.navbar-scrolled {
      padding: 0 12px;
      height: 70px;
    }
    .brand-logo-text {
      display: none;
    }
    .brand-logo-img {
      height: 36px;
    }
    .center-capsule-scaffold {
      display: flex !important;
      flex-grow: 1;
    }
    .atithi-search-pill-hub {
      padding: 4px 6px 4px 12px;
      width: 100%;
    }
    .main-search-input {
      width: 100%;
      min-width: 0;
      font-size: 13px;
    }
    .main-search-input:focus {
      width: 100%;
    }
    .capsule-v-divider {
      display: none;
    }
    .btn-capsule-filter-toggle {
      padding: 4px 6px;
      font-size: 0;
    }
    .btn-capsule-filter-toggle i {
      font-size: 14px;
    }
    .btn-search-magnifier {
      width: 32px;
      height: 32px;
      font-size: 11px;
    }
    .currency-globe-btn {
      display: none !important;
    }
    .nav-host-link {
      display: none !important;
    }
    .profile-trigger-pill {
      padding: 4px 5px;
      gap: 6px;
    }
    .profile-avatar-node {
      width: 30px;
      height: 30px;
      font-size: 12px;
    }
    .atithi-inline-filter-dropdown {
      position: fixed;
      top: 76px;
      left: 10px;
      right: 10px;
      width: auto;
      max-height: 80vh;
      overflow-y: auto;
    }
    .currency-grid-deck {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>

<!-- ==========================================
     NAVBAR MASTER ENGINE
     ========================================== -->
<nav class="atithi-navbar-master" id="masterAtithiNavbar">
  
  <!-- 1. LEFT BRAND LOGO -->
  <a class="brand-identity-anchor" href="/listings">
    <img src="/images/airbnbicon.png" alt="Atithi Logo" class="brand-logo-img">
    <span class="brand-logo-text">Atithi</span>
  </a>

  <!-- 2. CENTER CAPSULE SEARCH ENGINE WITH VOICE & ADVANCED FILTERS -->
  <div class="center-capsule-scaffold">
    <div class="atithi-search-pill-hub">
      <form action="/listings" method="GET" id="atithiLiveSearchForm" class="search-inline-form" autocomplete="off">
        
        <input 
          type="text" 
          name="search" 
          id="atithiSearchInput"
          placeholder="Search destination, villa or city..." 
          value="<%= (typeof currentFilters !== 'undefined' && currentFilters.search) ? currentFilters.search : '' %>"
          class="main-search-input"
        >
        
        <!-- Voice Search Button -->
        <button type="button" class="btn-voice-mic-trigger" id="voiceSearchTrigger" title="Search with Voice">
          <i class="bi bi-mic"></i>
        </button>

        <span class="capsule-v-divider">|</span>
        
        <!-- Advanced Filter Toggle -->
        <button class="btn-capsule-filter-toggle" type="button" id="inlineFilterDropdownBtnToggle">
          Filters <i class="bi bi-sliders"></i>
        </button>

        <!-- Submit Magnifier -->
        <button type="submit" class="btn-search-magnifier" aria-label="Search">
          <i class="bi bi-search"></i>
        </button>

        <!-- INLINE FILTER DROPDOWN SHEET -->
        <div class="atithi-inline-filter-dropdown" id="atithiInlineFilterDropdownPanel">
          
          <!-- Price Range Slider -->
          <div class="mb-4">
            <div class="d-flex justify-content-between align-items-center mb-2">
              <span class="filter-section-header m-0">Price Range</span>
              <span class="price-slider-label" id="inlineDisplayPriceLbl">
                max &#8377;<%= (typeof currentFilters !== 'undefined' && currentFilters.maxPrice) ? Number(currentFilters.maxPrice).toLocaleString('en-IN') : '1,00,000' %>
              </span>
            </div>
            <input 
              type="range" 
              class="atithi-filter-slider" 
              name="maxPrice" 
              min="1000" 
              max="100000" 
              step="500" 
              value="<%= (typeof currentFilters !== 'undefined' && currentFilters.maxPrice) ? currentFilters.maxPrice : '100000' %>" 
              id="inlineDropdownSliderInput"
            >
          </div>

          <!-- Category Filter Radios -->
          <div class="mb-4">
            <label class="filter-section-header">Property Type</label>
            <div class="category-pill-matrix">
              <% const categoriesList = ['Trending', 'Rooms', 'Amazing views', 'Mansions', 'Amazing pools', 'Beach', 'Cabins', 'Farms']; %>
              <% for(let cat of categoriesList) { %>
                <div>
                  <input 
                    type="radio" 
                    class="pill-check-input inline-auto-stream-trigger" 
                    name="category" 
                    id="cat_<%= cat.replace(/\s+/g, '_') %>" 
                    value="<%= cat %>"
                    <%= (typeof currentFilters !== 'undefined' && currentFilters.category === cat) ? 'checked' : '' %>
                  >
                  <label class="pill-check-label" for="cat_<%= cat.replace(/\s+/g, '_') %>"><%= cat %></label>
                </div>
              <% } %>
            </div>
          </div>

          <!-- Room Features Tag Checkboxes -->
          <div class="mb-3">
            <label class="filter-section-header">Room Features</label>
            <div class="category-pill-matrix">
              <% const tagsList = ['General', 'Bedroom', 'Kitchen', 'Bathroom', 'Exterior']; %>
              <% for(let tag of tagsList) { %>
                <div>
                  <input 
                    type="checkbox" 
                    class="pill-check-input inline-auto-stream-trigger" 
                    name="imageTag" 
                    id="tag_<%= tag %>" 
                    value="<%= tag %>"
                    <%= (typeof currentFilters !== 'undefined' && currentFilters.imageTag && currentFilters.imageTag.includes(tag)) ? 'checked' : '' %>
                  >
                  <label class="pill-check-label" for="tag_<%= tag %>"><%= tag %></label>
                </div>
              <% } %>
            </div>
          </div>

          <!-- Actions -->
          <div class="filter-actions-bottom">
            <a href="/listings" class="filter-clear-link">Clear all</a>
            <button type="button" class="btn-filter-apply" id="inlineFilterPanelCloseApplyBtn">Apply Filters</button>
          </div>

        </div>
      </form>

      <!-- Autocomplete Dropdown List -->
      <div class="atithi-autocomplete-panel" id="atithiAutocompletePanel"></div>
    </div>
  </div>

  <!-- 3. RIGHT UTILITY CONTROLS -->
  <div class="right-utility-deck">
    
    <a class="nav-host-link d-none d-lg-block" href="/listings/new">Join as a Host</a>

    <!-- Multi-Currency Selector Trigger -->
    <button class="currency-globe-btn" id="currencyModalTriggerBtn" title="Choose a currency">
      <i class="bi bi-globe2"></i> <span id="currentActiveCurrencyDisplay">INR (₹)</span>
    </button>

    <!-- User Profile Dropdown -->
    <div class="dropdown">
      <div class="profile-trigger-pill" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="bi bi-list fs-5" style="color: #334155;"></i>
        <div class="profile-avatar-node">
          <% if(!currUser) { %>
            <i class="bi bi-person-fill" style="font-size: 16px;"></i>
          <% } else if(currUser.avatar) { %>
            <img src="<%= currUser.avatar %>" alt="<%= currUser.username %>" referrerpolicy="no-referrer">
          <% } else { %>
            <%= currUser.username.charAt(0).toUpperCase() %>
          <% } %>
        </div>
      </div>

      <ul class="dropdown-menu dropdown-menu-end dropdown-menu-atithi">
        <% if(!currUser){ %>
          <li><a class="dropdown-item fw-bold" href="/signup"><i class="bi bi-person-plus text-primary"></i>Sign Up</a></li>
          <li><a class="dropdown-item" href="/login"><i class="bi bi-box-arrow-in-right text-success"></i>Log In</a></li>
          <li><hr class="dropdown-divider"></li>
          <li><a class="dropdown-item" href="/listings/new"><i class="bi bi-house-door"></i>Host your home</a></li>
          <li><a class="dropdown-item" href="/settings"><i class="bi bi-gear"></i>Settings</a></li>
          <li><a class="dropdown-item" href="/help"><i class="bi bi-question-circle"></i>Help Centre</a></li>
        <% } else { %>
          <li class="px-3 py-2">
            <span class="d-block text-secondary small fw-bold text-uppercase" style="font-size: 10px;">Logged In As</span>
            <span class="d-block fw-bold text-dark text-truncate" style="font-size: 14px;">@<%= currUser.username %></span>
          </li>
          <li><hr class="dropdown-divider"></li>
          <li><a class="dropdown-item" href="/bookings/my-bookings"><i class="bi bi-suit-case-fill text-info"></i>My Bookings</a></li>
          <li><a class="dropdown-item" href="/bookings/owner-dashboard"><i class="bi bi-speedometer2 text-warning"></i>Host Dashboard</a></li>
          <li><a class="dropdown-item" href="/listings/new"><i class="bi bi-house-add text-success"></i>Create New Listing</a></li>
          <li><a class="dropdown-item" href="/settings"><i class="bi bi-gear text-secondary"></i>Account Settings</a></li>
          <li><hr class="dropdown-divider"></li>
          <li><a class="dropdown-item text-danger fw-semibold" href="/logout"><i class="bi bi-box-arrow-right"></i>Log Out</a></li>
        <% } %>
      </ul>
    </div>

  </div>

</nav>

<!-- ==========================================
     🌍 5. GLOBAL CURRENCY SELECTION MODAL
     ========================================== -->
<div class="atithi-currency-modal-backdrop" id="atithiCurrencyModal">
  <div class="currency-modal-sheet" onclick="event.stopPropagation()">
    <div class="currency-modal-head">
      <h4>Choose a currency</h4>
      <button class="btn btn-sm btn-light rounded-circle" id="closeCurrencyModalBtn" style="width: 36px; height: 36px;">
        <i class="bi bi-x-lg"></i>
      </button>
    </div>

    <!-- Currency Options Grid -->
    <div class="currency-grid-deck">
      <div class="currency-option-card selected-curr" data-code="INR" data-symbol="₹">
        <span class="curr-card-name">Indian Rupee</span>
        <span class="curr-card-code">INR &middot; ₹</span>
      </div>
      <div class="currency-option-card" data-code="USD" data-symbol="$">
        <span class="curr-card-name">United States Dollar</span>
        <span class="curr-card-code">USD &middot; $</span>
      </div>
      <div class="currency-option-card" data-code="EUR" data-symbol="€">
        <span class="curr-card-name">Euro</span>
        <span class="curr-card-code">EUR &middot; €</span>
      </div>
      <div class="currency-option-card" data-code="GBP" data-symbol="£">
        <span class="curr-card-name">British Pound</span>
        <span class="curr-card-code">GBP &middot; £</span>
      </div>
      <div class="currency-option-card" data-code="JPY" data-symbol="¥">
        <span class="curr-card-name">Japanese Yen</span>
        <span class="curr-card-code">JPY &middot; ¥</span>
      </div>
      <div class="currency-option-card" data-code="AED" data-symbol="د.إ">
        <span class="curr-card-name">Emirati Dirham</span>
        <span class="curr-card-code">AED &middot; د.إ</span>
      </div>
      <div class="currency-option-card" data-code="CAD" data-symbol="$">
        <span class="curr-card-name">Canadian Dollar</span>
        <span class="curr-card-code">CAD &middot; $</span>
      </div>
      <div class="currency-option-card" data-code="AUD" data-symbol="$">
        <span class="curr-card-name">Australian Dollar</span>
        <span class="curr-card-code">AUD &middot; $</span>
      </div>
      <div class="currency-option-card" data-code="SGD" data-symbol="$">
        <span class="curr-card-name">Singapore Dollar</span>
        <span class="curr-card-code">SGD &middot; $</span>
      </div>
    </div>
  </div>
</div>

<!-- ==========================================
     JAVASCRIPT: SCROLL SHRINK, CURRENCY, VOICE, AJAX
     ========================================== -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const navbar = document.getElementById("masterAtithiNavbar");
    const searchInput = document.getElementById("atithiSearchInput");
    const suggestionPanel = document.getElementById("atithiAutocompletePanel");
    const filterBtnToggle = document.getElementById("inlineFilterDropdownBtnToggle");
    const filterDropdownPanel = document.getElementById("atithiInlineFilterDropdownPanel");
    const inlineSliderInput = document.getElementById("inlineDropdownSliderInput");
    const inlineDisplayPriceLbl = document.getElementById("inlineDisplayPriceLbl");
    const closeApplyBtn = document.getElementById("inlineFilterPanelCloseApplyBtn");
    const micBtn = document.getElementById("voiceSearchTrigger");
    
    // Currency modal variables
    const currTrigger = document.getElementById("currencyModalTriggerBtn");
    const currModal = document.getElementById("atithiCurrencyModal");
    const currClose = document.getElementById("closeCurrencyModalBtn");
    const currCards = document.querySelectorAll(".currency-option-card");
    const currDisplay = document.getElementById("currentActiveCurrencyDisplay");

    let debounceTimer;

    // 🚀 1. DYNAMIC SCROLL SHRINK & EXPAND LISTENER
    window.addEventListener("scroll", () => {
      if (window.scrollY > 20) {
        navbar.classList.add("navbar-scrolled");
      } else {
        navbar.classList.remove("navbar-scrolled");
      }
    });

    // 🚀 2. MULTI-CURRENCY CONVERSION ENGINE & MODAL CONTROLS
    const exchangeRates = {
      INR: { symbol: "₹", rate: 1 },
      USD: { symbol: "$", rate: 0.012 },
      EUR: { symbol: "€", rate: 0.011 },
      GBP: { symbol: "£", rate: 0.0095 },
      JPY: { symbol: "¥", rate: 1.85 },
      AED: { symbol: "د.إ", rate: 0.044 },
      CAD: { symbol: "CA$", rate: 0.016 },
      AUD: { symbol: "AU$", rate: 0.018 },
      SGD: { symbol: "SG$", rate: 0.016 }
    };

    function applyCurrencyConversion(code) {
      const config = exchangeRates[code] || exchangeRates.INR;
      localStorage.setItem("atithi_currency", code);

      if (currDisplay) {
        currDisplay.textContent = `${code} (${config.symbol})`;
      }

      document.querySelectorAll("[data-base-price]").forEach(el => {
        const inrPrice = parseFloat(el.getAttribute("data-base-price"));
        if (!isNaN(inrPrice)) {
          const converted = Math.round(inrPrice * config.rate);
          el.innerHTML = `<bdi>${config.symbol}</bdi>${converted.toLocaleString()}`;
        }
      });

      document.querySelectorAll("[data-tax-base]").forEach(el => {
        const taxBase = parseFloat(el.getAttribute("data-tax-base"));
        if (!isNaN(taxBase)) {
          const converted = Math.round(taxBase * config.rate);
          el.innerHTML = `<bdi>${config.symbol}</bdi>${converted.toLocaleString()}`;
        }
      });

      if (inlineDisplayPriceLbl && inlineSliderInput) {
        const convertedMax = Math.round(parseFloat(inlineSliderInput.value) * config.rate);
        inlineDisplayPriceLbl.innerHTML = `max <bdi>${config.symbol}</bdi>${convertedMax.toLocaleString()}`;
      }

      window.dispatchEvent(new CustomEvent("atithi_currency_updated", { detail: { code, config } }));
    }

    if (currTrigger && currModal && currClose) {
      currTrigger.addEventListener("click", () => {
        currModal.classList.add("active-curr-modal");
      });

      currClose.addEventListener("click", () => {
        currModal.classList.remove("active-curr-modal");
      });

      currModal.addEventListener("click", (e) => {
        if (e.target === currModal) {
          currModal.classList.remove("active-curr-modal");
        }
      });

      currCards.forEach(card => {
        card.addEventListener("click", () => {
          currCards.forEach(c => c.classList.remove("selected-curr"));
          card.classList.add("selected-curr");
          const code = card.dataset.code;
          applyCurrencyConversion(code);
          currModal.classList.remove("active-curr-modal");
        });
      });

      const savedCurrency = localStorage.getItem("atithi_currency") || "INR";
      const activeCard = document.querySelector(`.currency-option-card[data-code="${savedCurrency}"]`);
      if (activeCard) {
        currCards.forEach(c => c.classList.remove("selected-curr"));
        activeCard.classList.add("selected-curr");
      }
      applyCurrencyConversion(savedCurrency);
    }

    // 🚀 3. FILTER PANEL TOGGLE
    if (filterBtnToggle && filterDropdownPanel) {
      filterBtnToggle.addEventListener("click", (e) => {
        e.stopPropagation();
        if (suggestionPanel) suggestionPanel.style.display = "none";
        const isShown = filterDropdownPanel.style.display === "block";
        filterDropdownPanel.style.display = isShown ? "none" : "block";
        filterBtnToggle.classList.toggle("active", !isShown);
      });
    }

    // 🚀 4. REALTIME GRID RE-HYDRATOR
    window.appendTextHtmlGrid = function() {
      const form = document.getElementById("atithiLiveSearchForm");
      if (!form) return;
      const formData = new FormData(form);
      const searchParams = new URLSearchParams(formData);

      if (window.location.pathname === "/listings" || window.location.pathname === "/") {
        fetch(`/listings?${searchParams.toString()}`)
          .then(res => res.text())
          .then(html => {
            const parser = new DOMParser();
            const doc = parser.parseFromString(html, 'text/html');
            const newContent = doc.querySelector('.row') || doc.querySelector('[class*="row-cols"]');
            const currentContent = document.querySelector('.row') || document.querySelector('[class*="row-cols"]');
            if (newContent && currentContent) {
              currentContent.innerHTML = newContent.innerHTML;
              const currentCurr = localStorage.getItem("atithi_currency") || "INR";
              applyCurrencyConversion(currentCurr);
            }
          });
      }
    };

    if (inlineSliderInput && inlineDisplayPriceLbl) {
      inlineSliderInput.addEventListener("input", (e) => {
        const savedCurr = localStorage.getItem("atithi_currency") || "INR";
        const config = exchangeRates[savedCurr] || exchangeRates.INR;
        const converted = Math.round(Number(e.target.value) * config.rate);
        inlineDisplayPriceLbl.innerHTML = `max <bdi>${config.symbol}</bdi>${converted.toLocaleString()}`;
      });
      inlineSliderInput.addEventListener("change", () => window.appendTextHtmlGrid());
    }

    document.querySelectorAll(".inline-auto-stream-trigger").forEach(el => {
      el.addEventListener("change", () => window.appendTextHtmlGrid());
    });

    if (closeApplyBtn) {
      closeApplyBtn.addEventListener("click", () => {
        filterDropdownPanel.style.display = "none";
        filterBtnToggle.classList.remove("active");
      });
    }

    // 🚀 5. VOICE SEARCH
    if (micBtn && ('webkitSpeechRecognition' in window || 'SpeechRecognition' in window)) {
      const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
      const recognition = new SpeechRecognition();
      recognition.continuous = false;
      recognition.lang = 'en-IN';

      micBtn.addEventListener("click", () => {
        try {
          recognition.start();
          micBtn.classList.add("recording-active");
        } catch(err) {
          recognition.stop();
          micBtn.classList.remove("recording-active");
        }
      });

      recognition.onresult = (event) => {
        const transcript = event.results[0][0].transcript;
        if (searchInput) {
          searchInput.value = transcript;
          window.appendTextHtmlGrid();
          document.getElementById("atithiLiveSearchForm").submit();
        }
        micBtn.classList.remove("recording-active");
      };

      recognition.onerror = () => micBtn.classList.remove("recording-active");
      recognition.onend = () => micBtn.classList.remove("recording-active");
    }

    // 🚀 6. LIVE SEARCH AUTOCOMPLETE
    if (searchInput && suggestionPanel) {
      searchInput.addEventListener("input", () => {
        clearTimeout(debounceTimer);
        if (filterDropdownPanel) {
          filterDropdownPanel.style.display = "none";
          filterBtnToggle.classList.remove("active");
        }

        const query = searchInput.value.trim().toLowerCase();
        if (query.length < 1) {
          suggestionPanel.style.display = "none";
          window.appendTextHtmlGrid();
          return;
        }

        debounceTimer = setTimeout(() => {
          window.appendTextHtmlGrid();

          fetch(`/listings?search=${encodeURIComponent(query)}&json=true`)
            .then(res => res.json())
            .then(data => {
              suggestionPanel.innerHTML = "";
              if (!data.success || !data.listings || data.listings.length === 0) {
                suggestionPanel.innerHTML = `<div class="text-muted small p-3 text-center">No stays found matching "${query}"</div>`;
                suggestionPanel.style.display = "block";
                return;
              }

              const savedCurr = localStorage.getItem("atithi_currency") || "INR";
              const config = exchangeRates[savedCurr] || exchangeRates.INR;

              data.listings.slice(0, 5).forEach(listing => {
                const title = listing.title || "Atithi Stay";
                const location = listing.location || "India";
                
                let price = "N/A";
                if (listing.price) {
                  const converted = Math.round(Number(listing.price) * config.rate);
                  price = `<bdi>${config.symbol}</bdi>${converted.toLocaleString()}`;
                }

                const imgSrc = (listing.images && listing.images.length > 0) 
                  ? listing.images[0].url 
                  : (listing.image && listing.image.url ? listing.image.url : "https://images.unsplash.com/photo-1584132967334-10e028bd69f7");

                const item = document.createElement("div");
                item.className = "suggestion-item";
                item.innerHTML = `
                  <img src="${imgSrc}" class="suggestion-thumb" alt="stay">
                  <div class="flex-grow-1 min-w-0">
                    <div class="suggestion-title text-truncate">${title}</div>
                    <div class="suggestion-subtitle text-truncate">${location}</div>
                  </div>
                  <div class="suggestion-price">${price}</div>
                `;

                item.addEventListener("click", () => {
                  window.location.href = `/listings/${listing._id}`;
                });

                suggestionPanel.appendChild(item);
              });

              suggestionPanel.style.display = "block";
            })
            .catch(() => {
              suggestionPanel.style.display = "none";
            });
        }, 250);
      });
    }

    // Dismiss on click outside
    document.addEventListener("click", (e) => {
      if (filterDropdownPanel && !filterDropdownPanel.contains(e.target) && !filterBtnToggle.contains(e.target)) {
        filterDropdownPanel.style.display = "none";
        filterBtnToggle.classList.remove("active");
      }
      if (suggestionPanel && !searchInput.contains(e.target) && !suggestionPanel.contains(e.target)) {
        suggestionPanel.style.display = "none";
      }
    });
  });
</script>